import '../components/_navbar.scss';
import '../data/navbar-options.json';

const DropdownNavbarMenu: React.FC = () => {

    let menuOptions = JSON.parse(props.menuoptions);
    
    function dropdownItens(){
        <a>
            aaa
        </a>
    }

    return (
        {dropdownItens}
    );
  };
  
  export default DropdownNavbarMenu;